
function Body() {
    /*return(
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-45 p-8">
            {cards.map((card) => (
        <Card
          key={card.skill}
          skill={card.skill}
          description={card.description}
          image={card.image}
          onClick={() => console.log(`${card.skill} clicked`)}
        />
      ))}
        </div>
    );*/
}

export default Body